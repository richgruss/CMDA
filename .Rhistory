age <- c(1,2,3,4,5,6,7,8)
weight <- c(5,6,7,2,3,4,9,8)
mean(weight)
mean(age)
cor(age,weight)
plot(age,weight)
weight <- c(12,16,19,32,55,100,200,600)
plot(age,weight)
cor(age,weight)
help.start()
cwd
pwd
getwd()
setwd('/Users/rgruss/git/CMDA')
#The health insurance customer data
load('exampleData.rData')
View(custdata)
summary(custdata$state.of.res)
summary(income)
summary(custdata.income)
summary(custdata.Income)
summary(custdata$Income)
summary(custdata(is.na(custdata$Income)))
summary(custdata(is.na(custdata$Income), "housing.type"))
summary(custdata(is.na(custdata$Income), c("housing.type"))
summary(custdata(is.na(custdata$Income), c("housing.type")))
summary(custdata(is.na(custdata$Income), c("housing.type", "is.employed")))
summary(custdata[is.na(custdata$Income), c("housing.type", "is.employed")])
summary(custdata[is.na(custdata$Income), c("housing.type")])
summary(custdata[is.na(custdata$Income), "housing.type"])
summary(custdata[!is.na(custdata$Income), "housing.type"])
meanincome <- mean(custdata$Income)
meanincome
meanincome <- mean(custdata$Income, na.rm=T)
Income.fix <- ifelse(is.na(custdata$Income), meanincome, custdata$Income)
summary(Income.fix)
breaks <- c(0, 10000, 50000, 100000, 250000, 100000)
Income.groups <=- cut(custdata$income, breaks=breaks, include.lowest=T)
Income.groups <= cut(custdata$income, breaks=breaks, include.lowest=T)
Income.groups <- cut(custdata$income, breaks=breaks, include.lowest=T)
breaks <- c(0, 10000, 50000, 100000, 250000, 1000000)
Income.groups <- cut(custdata$income, breaks=breaks, include.lowest=T)
